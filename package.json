{
  "name": "tasks-front-end",
  "version": "1.0.0",
  "description": "front end for the tasks app",
  "main": "index.js",
  "scripts": {
    "test": "test",
    "build": "esbuild index.js --bundle  --outfile=bundle.js --watch"
  },
  "author": "ruben wilson",
  "license": "ISC",
  "dependencies": {
    "jest": "^29.1.2",
    "jest-environment-jsdom": "^29.1.2",
    "jest-fetch-mock": "^3.0.3"
  }
}
